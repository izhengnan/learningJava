# 服务器配置
server:
  port: 8080

spring:
  application:
    name: shipment
  datasource:
    url: jdbc:mysql://localhost:8864/shipment
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: shipment
    password: 58jipnAmf7yKXJrc

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  mapper-locations: classpath:com/itheima/shipment/mapper/*.xml