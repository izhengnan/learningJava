<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <insert id="submitOrder" useGeneratedKeys="true" keyProperty="id">
        insert into orders(number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status, amount, remark, phone, address, user_name, consignee, cancel_reason, rejection_reason, cancel_time, estimated_delivery_time, delivery_status, delivery_time, pack_amount, tableware_number, tableware_status)
        values
            (#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod}, #{payStatus}, #{amount}, #{remark}, #{phone}, #{address}, #{userName}, #{consignee}, #{cancelReason}, #{rejectionReason}, #{cancelTime}, #{estimatedDeliveryTime}, #{deliveryStatus}, #{deliveryTime}, #{packAmount}, #{tablewareNumber}, #{tablewareStatus})
    </insert>


    <update id="update" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' ">
                cancel_reason=#{cancelReason},
            </if>
            <if test="rejectionReason != null and rejectionReason!='' ">
                rejection_reason=#{rejectionReason},
            </if>
            <if test="cancelTime != null">
                cancel_time=#{cancelTime},
            </if>
            <if test="payStatus != null">
                pay_status=#{payStatus},
            </if>
            <if test="payMethod != null">
                pay_method=#{payMethod},
            </if>
            <if test="checkoutTime != null">
                checkout_time=#{checkoutTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deliveryTime != null">
                delivery_time = #{deliveryTime}
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="updateList">
        <foreach collection="ordersList" item="list" separator=";">
            update orders
            set
            <trim suffixOverrides=",">
                <if test="list.cancelReason != null and list.cancelReason!='' ">
                    cancel_reason= #{list.cancelReason},
                </if>
                <if test="list.rejectionReason != null and list.rejectionReason!='' ">
                    rejection_reason= #{list.rejectionReason},
                </if>
                <if test="list.cancelTime != null">
                    cancel_time= #{list.cancelTime},
                </if>
                <if test="list.payStatus != null">
                    pay_status= #{list.payStatus},
                </if>
                <if test="list.payMethod != null">
                    pay_method= #{list.payMethod},
                </if>
                <if test="list.checkoutTime != null">
                    checkout_time=#{list.checkoutTime},
                </if>
                <if test="list.status != null">
                    status = #{list.status},
                </if>
                <if test="list.deliveryTime != null">
                    delivery_time = #{list.deliveryTime}
                </if>
            </trim>
            where id = #{list.id}
        </foreach>

    </update>
    <select id="selectHistoryOrders" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            user_id = #{userId}
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by order_time desc
    </select>
    <select id="selectOrderDetail" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            id = #{id}
        </where>
    </select>
    <select id="conditionSearchOrder" resultType="com.sky.vo.OrderVO">
        select * from orders
        <where>
            <if test="number!=null" >and number like concat('%',#{number},'%')</if>
            <if test="phone!=null" >and phone like concat('%',#{number},'%')</if>
            <if test="status!=null" >and status=#{status}</if>
        </where>
        order by order_time desc
    </select>
    <select id="orderStatistics" resultType="java.lang.Integer">
        select status from orders
    </select>
    <select id="getByStatusAndOrderTime" resultType="com.sky.entity.Orders">
        select * from orders where status=#{status} and order_time &lt; #{orderTime}
    </select>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(*) from orders
        <where>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
</mapper>