<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">



    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="dishId" open="(" close=")" separator=",">
            #{dishId}
        </foreach>
    </select>
    <insert id="addSetmealDishes">
        insert into setmeal_dish (id ,setmeal_id, dish_id, name, price, copies)values
        <foreach collection="setmealDishes" item="setmealDish" separator=",">
            (#{setmealDish.id},#{setmealDish.setmealId},#{setmealDish.dishId},#{setmealDish.name},#{setmealDish.price},#{setmealDish.copies})
        </foreach>
    </insert>
    <delete id="deleteSetmealDishList">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <select id="selectSetmealDishById" resultType="com.sky.entity.SetmealDish">
        select * from setmeal_dish where setmeal_id = #{id};
    </select>
    <select id="selectSetmealDishByIdList" resultType="java.lang.Integer">
        select status from setmeal where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectSetmealDishStatusBySetmealId" resultType="java.lang.Integer">
        select d.status from setmeal_dish sd left join dish d on sd.dish_id = d.id
        where sd.setmeal_id = #{id}
    </select>

</mapper>